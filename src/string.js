const stringLength = string => {
    const str = "Hello";
    return str;
}

const reverseString = (string) => string.toLowerCase().split("").reverse.join("");

module.exports = {stringLength, reverseString}